파이썬에서 아래 코드의 출력은?

def f(x, acc=[]):
    acc.append(x)
    return acc
f(1); f(2)
print(f(3))

마지막 줄에 ANSWER=<출력값> 형식으로 적어라. 리스트는 [1, 2, 3] 처럼 파이썬 표기 그대로.

도구를 쓸 수 없다. 실행해 보지 말고 추론만으로 답하라. 반드시 마지막 줄에 위 형식으로 ANSWER= 한 줄을 적어라.
