파이썬 함수 하나를 써라.

chunk_by_bytes(items, limit) -> list[list[str]]
  문자열 리스트를 순서를 지키며 묶는다. 각 묶음의 UTF-8 바이트 길이 합이 limit 이하여야 한다.
  혼자서도 limit 을 넘는 문자열은 그것만으로 한 묶음이 된다.
  빈 입력은 빈 리스트다. limit 이 0 이하면 ValueError 를 낸다.

표준 라이브러리만 쓴다. 도구를 쓸 수 없으니 실행해 보지 말고 써라.
설명 없이 ```python 코드블록 하나만 내라. 함수 밖의 예제나 print 는 넣지 마라.
