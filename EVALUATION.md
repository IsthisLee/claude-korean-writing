# korean-writing 합격 기준

목적: **Claude Code가 쓰는 모든 한국어의 품질을 맡는다.** 평소 답변에는 상시 규칙이 붙고 글 작성 요청은 스킬이 처음부터 맡고 이미 쓴 글은 윤문 스킬이 다듬고 README는 절 구성을 잡고 `.md` 편집은 훅이 검사한다.

이 목적이 달성됐는지 판정하는 기준이다. 하나라도 미달이면 고친다.

## A. 훅 정확도

정답 데이터는 **이 세션에서 내가 실제로 쓴 어색한 문장 10건**(`ground-truth.json`)과 **같은 맥락의 정상 문장 5건**(`clean.json`)이다. 합성 예문이 아니라 실측된 실패다.

| #   | 기준                                               | 임계      |
| --- | -------------------------------------------------- | --------- |
| A1  | 위반 문장 검출률                                   | **10/10** |
| A2  | 정상 문장 오탐                                     | **0/5**   |
| A3  | 실제 프로젝트 `.md` 편집 시 오탐                   | **0건**   |
| A4  | 분류 정확도 — 검출된 항목이 맞는 코드로 보고되는가 | **10/10** |

A2가 A1보다 중요하다. 게이트가 정상 작업을 막으면 사람이 게이트를 꺼버린다.

## B. 스킬 트리거

| #   | 기준                                                               | 임계    |
| --- | ------------------------------------------------------------------ | ------- |
| B1  | 이 세션에서 실제로 나온 글 작성 요청 5종에 description 이 걸리는가 | **5/5** |
| B2  | 코드 작업 요청에는 안 걸리는가                                     | **0/5** |

B1 대상: "슬랙 내 캐쥬얼하게 확인 요청드리려는데", "비개발자 눈높이로 정리해줄래", "노션용으로 정리", "커밋 메시지 써줘", "README 작성"

## C. 스킬 내용 유효성 — 핵심

**스킬을 로드하면 실제로 더 나은 글이 나오는가.** `superpowers:writing-skills`의 Iron Law 는 "스킬 없이 실패하는 것을 보지 않았으면 그 스킬이 옳은 것을 가르치는지 알 수 없다"이다.

| #   | 기준                                                                                | 임계         |
| --- | ----------------------------------------------------------------------------------- | ------------ |
| C1  | 정답 데이터 10건을 스킬 규칙으로 교정할 때, 각 위반에 대응하는 처방이 스킬에 있는가 | **10/10**    |
| C2  | 교정 결과가 사실을 바꾸지 않는가 (수치·고유명사 보존)                               | **10/10**    |
| C3  | 스킬 문서 자신이 자기 규칙을 지키는가                                               | 위반 **0건** |

**C4 스킬 유무 비교 (2026-09-10).** 같은 프롬프트 4개(슬랙 안내문, QA 보고서, 배포 절차 문서, 팀 규칙 문서)를 `claude -p` 로 플러그인이 로드된 기본 설정과 사용자 설정을 뺀 상태(`--setting-sources project`)로 각각 돌렸다. 모델은 claude-opus-5. 유효 표본 7건(1건은 max-turns 초과로 빈 결과)이 전부 훅을 통과했고 줄표·첫째둘째·관용구·승패·것들 표지가 0이었다. 이 모델과 이 표본으로는 스킬의 생성 단계 효과를 구분하지 못했다. 실측 문서의 줄표 34~~207개는 2026년 6~~8월 산출물이라 그때의 모델과 설정에서 나온 것이다. 훅은 모델과 설정이 바뀌어도 남는 안전망이고 스킬의 효과는 표지가 나오는 모델·프롬프트에서 다시 재야 한다.

**C5 상시 규칙 주입 (2026-09-10).** 스킬이 뜨지 않는 평소 답변에서 `hooks-handlers/always-on.md` 주입이 문체를 개선하는지 쟀다. 기준을 실행 전에 적어 두고 결과를 보고 바꾸지 않았다. 프롬프트 12개(함수 설명, 에러 진단, diff 설명, 설계 추천, 리팩터링, 테스트 실패, 성능, 개념 설명, 로그 분석, PR 리뷰, 설계 점검, git 충돌)에 조건 2개와 표본 2개로 48건을 생성하고 같은 모델에 순서를 바꿔 두 번씩 블라인드로 판정했다(48회). 모델은 claude-fable-5-1, effort xhigh.

| 측정                              | 기준선  | 주입    |
| --------------------------------- | ------- | ------- |
| 블라인드 쌍대 승 (24쌍)           | 0       | **21**  |
| 무승부                            | 3       | 3       |
| 훅 검사기 적중                    | 2       | **0**   |
| 정규식 원시 패턴 합계             | 5       | **0**   |
| 굵게 `**` 사용                    | 174     | **110** |
| 반말 종결 (재확인 6표본)          | 8       | **0**   |
| 기술 오류 (문체를 뺀 별도 검사)   | 34      | 32      |

12개 프롬프트 전부에서 주입 쪽이 이기거나 비겼다. 판정이 기준선에서 반복해 지적한 것은 굵은 소제목 남발, `결론부터`·`정리하면` 같은 상투 문두, 영어 직역(`화면을 얼린다`, `배치는 굶습니다`), 형식명사, 문체 혼용이다. 사실 정확도는 심각한 오류가 양쪽 0건이고 사소한 과장은 34 대 32 로 사실상 같다. 길이 비율의 중앙값은 1.03 이다.

주입문의 높임은 따로 고쳤다. 초안이 해라체로 적혀 있어 답변까지 해라체로 기울었고 `상대에게 맞춘다` 로 적은 판은 반말 질문에 반말로 답해 더 나빴다(6표본에서 반말 종결 18회). `평소대로 존댓말로 답한다` 로 고정하니 0회가 됐다.

**한계.** 판정 모델과 생성 모델이 같다. 표본이 2다. 단일 턴이라 긴 대화에서 효과가 약해지는지는 재지 않았다. 정규식 훅은 평소 답변에서 표본당 0.21건만 잡으므로, 이 검사기로는 답변 품질의 회귀를 감시하지 못한다.

**C6 규칙집 커버리지 (2026-09-10 신설).** 스킬이 im-not-ai 규칙집을 얼마나 덮는가. 그 규칙집의 `quick: true` 항목 61개는 표층 신호만으로 판정할 수 있어 생성 단계에 그대로 옮길 수 있다. 항목마다 고유 표지를 정해 `SKILL.md` 에서 찾는 방식으로 셌다.

| 시점 | quick 항목 처방 보유 | 비고 |
| --- | --- | --- |
| 이번 개정 전 | 61개 중 **21개** | S1 가운데 A-3·A-16·C-8·C-11·D-3 이 통째로 빠져 있었다 |
| 이번 개정 후 | 61개 중 **60개** | 빠진 하나는 G-3. 규칙집이 "실증 부족 — hold" 로 표시한 항목이라 뺐다 |

빠져 있던 것 가운데 둘이 그 규칙집의 실측 최강 신호다. C-8 부정 대구는 밀도가 사람 대비 9.2배(개인 블로그 대비 18배)이고 세 모델과 세 과업 조건에서 모두 재현된 유일한 항목이다. C-11 연결어미 뒤 쉼표는 KatFish(ACL 2025) 에세이 측정에서 사람 4.10% 대 AI 19.83% 로 단일 지표 분리도가 가장 크다.

**C7 생성 단계 비교 (2026-09-10).** 규칙을 늘린 것이 실제로 생성물을 바꾸는지 쟀다. 프롬프트 넷(배포 절차 공지, 실패율 보고서, 릴리스 노트, 코드 리뷰 문화 문서)에 조건과 표본 다섯으로 생성했다. `claude -p --append-system-prompt "$(cat SKILL.md)" --strict-mcp-config --setting-sources ""`, 모델은 claude-opus-5.

조건은 넷이다. 규칙 없음, 옛 스킬, 새 스킬 초판, 새 스킬 최종판이다. 초판과 최종판을 나눈 이유는 아래에 있다.

| 측정 | 규칙 없음 | 옛 스킬 | 새 스킬 초판 | 새 스킬 최종판 |
| --- | --- | --- | --- | --- |
| 표본당 AI 티 표지 | 0.95 | 1.80 | **0.25** | 0.90 |
| 연결어미 쉼표 비율 평균 | 18.9% | 38.8% | **1.9%** | 11.1% |
| 문장 길이 표준편차 | 16.16 | 16.28 | 12.93 | **18.97** |
| 최장 문장 길이 | 68.8 | 70.3 | 60.9 | **81.1** |
| 100자 넘는 문장 (표본당) | 0.05 | 0.00 | 0.00 | **0.10** |

**정규식 계수만으로는 품질을 말할 수 없다.** 초판은 표지를 1.80에서 0.25로 줄였는데, 저장소의 기존 판정 기준(`docs/experiments/always-on/judge_one.sh` 의 감점 목록)으로 블라인드 쌍대 판정을 돌리니 옛 스킬과 구별되지 않았다. 20쌍을 순서를 바꿔 두 번씩 물어 일관되게 갈린 것이 초판 5승 옛 스킬 6승이고 아홉 쌍은 순서에 따라 답이 뒤집혔다. 판정 기준에는 이번에 넣은 대구·쉼표 항목이 없다. 자기가 만든 지표만 좋아졌는지 보려고 일부러 그대로 뒀고, 결과는 그랬다.

**판정자가 되풀이해 지적한 것은 리듬이었다.** "문장 길이가 고르게 짧아 기계적으로 읽힌다", "글 2는 문장 길이도 고르지 않아 더 자연스럽다" 같은 문장이 반복해 나왔다. 재 보니 문장 길이 표준편차가 16.28에서 12.93으로 떨어져 있었다. 규칙을 지키려고 문장을 짧게 끊은 것이고, 길이 균일성은 규칙집의 E-1 항목이다. 하나를 고치면서 다른 하나를 만든 셈이다.

그래서 `SKILL.md` 에 「리듬」 절을 넣었다. 문단마다 100자 안팎의 긴 문장을 하나 두라는 것과, 규칙은 표현을 빼라는 것이지 문장을 자르라는 뜻은 아니라는 것 둘이다. 최종판은 표준편차 18.97로 네 조건 중 가장 높다. 연결어미 쉼표는 1.9%에서 11.1%로 올랐는데, 사람이 쓴 글의 이 머신 실측 중앙값이 약 15% 이므로 1.9%는 오히려 사람보다 적었다. 목표는 0이 아니고 사람 수준이다.

**블라인드 쌍대 판정.** 생성은 claude-opus-5, 판정은 claude-fable-5-1 이다. 판정자와 생성자를 다른 모델로 뒀다(C5 의 한계였던 부분이다). 쌍마다 순서를 바꿔 두 번 묻고, 두 번 같은 답이 나온 쌍만 승패로 셌다.

| 비교 | 일관 승패 | 이항 단측 p |
| --- | --- | --- |
| 최종판 대 옛 스킬 | **12승 4패** (갈림 4) | **0.038** |
| 최종판 대 초판 | 10승 5패 (갈림 5) | 0.151 |
| 초판 대 옛 스킬 | 5승 6패 (갈림 9) | 0.726 |

최종판이 이 저장소의 종전 스킬을 이긴 것은 유의하다. 초판은 이기지 못했다. 리듬 절이 그 차이를 만들었다는 것이 최종판 대 초판 비교인데 방향만 맞고 유의하지는 않다.

**한계.** 모델 하나로 생성했고 프롬프트가 여덟이다. 판정자를 사람이 아닌 모델이 맡았다. 스킬을 시스템 프롬프트로 붙여 돌렸으므로 실제 스킬 로드 경로와 완전히 같지는 않다. 갈림 쌍이 20~45% 나오는데 이는 두 글의 차이가 작다는 뜻이기도 하다.

**옛 스킬이 규칙 없음보다 나빴다.** 연결어미 쉼표가 32 대 14 이고 비율로는 38.8% 대 18.9% 다. 왜 그런지는 확인하지 못했다. 옛 스킬 본문의 쉼표 비율 자체는 10% 로 낮으므로 본문 흉내는 설명이 되지 않는다. 줄표 처방이 "쉼표나 문장 분리로 바꾼다" 라 쉼표 쪽으로 민다는 가설이 있으나, 새 스킬도 같은 문장을 그대로 두고 쉼표가 떨어졌으므로 그 가설만으로는 부족하다.

**C9 im-not-ai 수준에 닿았는가 (2026-09-10).** 이 플러그인의 목표는 스킬의 품질이 im-not-ai 수준이 되거나 그보다 높은 것이다. 그쪽은 이미 쓴 글을 고치는 파이프라인이므로, 비교는 「규칙 없이 쓴 글을 im-not-ai 로 윤문한 것」 대 「스킬로 처음부터 쓴 글」로 세웠다.

**두 조건에서 쟀다.** 단문은 400~600자 업무 문서 여덟 종(공지·보고서·릴리스 노트·문화 문서·장애 공지·회의록·기획안·가격 안내)이고, 장문은 1,500자 칼럼·에세이·리포트 여섯 종이다. 장문을 따로 둔 이유는 윤문 파이프라인이 원래 강한 자리가 그쪽이기 때문이다.

**단문에서는 Fast Path 한 콜, 장문에서는 정밀 3콜을 재현했다.** 정밀 모드는 진단·윤문·마무리 세 콜이고, 결합 입력은 저장소의 실제 스크립트 `scripts/prepare_monolith_input.py` 가 만들었다. 시스템 프롬프트는 `agents/humanize-diagnostician.md`·`humanize-monolith.md`·`humanize-finalizer.md` 와 규칙집 원문이다. 도구가 없는 세션이라 파일 입출력만 표준입출력으로 바꿨다.

| 윤문 입력 | 경로 | 변경률 중앙값 | 거의 안 고침 |
| --- | --- | --- | --- |
| 규칙 없이 쓴 단문 | Fast Path 1콜 | 0.3% | 20건 중 13 |
| 규칙 없이 쓴 장문 | 정밀 3콜 | 1.0% | 24건 중 13 |

**진단을 붙여도 변경률이 오르지 않았다.** 그쪽 문서는 진단 없이 윤문만 하면 0.5%로 no-op 이 되고 진단을 붙이면 11%로 뛴다고 적고 있는데, 여기서는 1.0% 였다. 재현이 잘못됐는지 보려고 진단 산출물을 열어 봤다. 5KB 짜리 진단이 taxonomy ID 와 정량 앵커를 달고 나왔고 shim 이 결합 입력 앞머리에 실었다. 재현은 맞았다. 진단이 스스로 내린 결론이 "처방 없음" 이었다. 한 건의 진단은 `C-11 0건·C-12 0건은 사람 글 방향의 강한 증거` 라고 적었다. **claude-opus-5 가 2026년에 쓰는 한국어가 이미 그 규칙집이 겨냥하는 상태에서 벗어나 있다는 뜻으로 읽는다.**

**표지 밀도 (장문, 1000자당).**

| 항목 | 규칙 없음 | 스킬 | im-not-ai 윤문본 |
| --- | --- | --- | --- |
| C-8 대구 | 0.42 | **0.22** | 0.39 |
| C-11 연결어미 쉼표 | 0.69 | 0.73 | **0.60** |
| I-1·3 것 구문 | 0.14 | **0.04** | 0.14 |
| F-5 ~적 N | 0.21 | 0.29 | **0.21** |
| 합계 | 1.49 | **1.32** | 1.37 |
| 문장 길이 표준편차 | 21.42 | 20.81 | 19.05 |

**1차 판정은 동등에서 멈췄다.** 단문 32쌍은 claude-fable-5-1 이, 장문 24쌍은 claude-opus-5 가 판정했다. 쌍마다 순서를 바꿔 두 번 묻고 두 번 같은 답이 나온 쌍만 셌다. 결과는 41쌍에서 스킬 22승 im-not-ai 19승이었다(단측 p=0.378). 우열을 말할 수 없는 자리다.

**진 쌍의 이유를 읽어 세 가지를 고쳤다.** 판정자가 남긴 문장이 세 갈래로 모였고 셋 다 실측으로 확인됐다.

| 판정자가 지적한 것 | 실측 | 규칙에 있었나 |
| --- | --- | --- |
| 말미의 분량 보고와 후속 제안 | 표본당 0.29·0.42회 | 없었다 |
| 소제목으로 잘게 나눔 | 헤딩 표본당 1.21줄 (규칙 없음·im-not-ai 는 0.62) | 없었다 |
| 문단마다 비슷한 리듬으로 끝남 | 문단 끝 문장 길이 표준편차 14.58 (규칙 없음 18.13, im-not-ai 16.24) | 문서 전체 리듬만 있었다 |

문서 전체의 문장 길이 표준편차는 스킬이 20.94로 im-not-ai 의 19.22보다 높았다. 고른 것은 문단을 닫는 문장이었고 그 축을 보지 않으면 안 보인다. `SKILL.md` 에 「요청한 글만 낸다」 절을 새로 넣고, 산문에 소제목을 달지 않는 규칙과 문단 끝 문장 길이를 돌려 쓰는 규칙을 더했다.

| 측정 | 규칙 없음 | 1차 스킬 | **고친 스킬** | im-not-ai |
| --- | --- | --- | --- | --- |
| 분량 보고 (표본당) | 0.50 | 0.29 | **0.00** | 0.38 |
| 후속 제안 (표본당) | 0.54 | 0.42 | **0.00** | 0.38 |
| 헤딩 (표본당) | 0.62 | 1.21 | **0.00** | 0.62 |
| 수평선 (표본당) | 1.58 | 1.62 | **0.04** | 1.38 |
| 문단 끝 길이 표준편차 | 18.13 | 14.58 | **16.67** | 16.24 |

**2차 판정에서 우위가 확인됐다.** 같은 프롬프트와 같은 im-not-ai 산출물에 고친 스킬을 붙였다. 판정은 claude-opus-5, 순서를 바꿔 두 번씩이다.

| 조건 | 스킬 승 | im-not-ai 승 | 갈림 | 이항 단측 p |
| --- | --- | --- | --- | --- |
| 단문 32쌍 | **28** | 0 | 4 | 0.0000 |
| 장문 24쌍 | **21** | 0 | 3 | 0.0000 |
| 합산 56쌍 | **49** | 0 | 7 | 2 의 -49 제곱 |

**마무리 문구 덕이 아니다.** 장문에서 im-not-ai 쪽에만 어시스턴트 마무리가 남아 있었으므로(24건 중 14건, 스킬은 0건) 그것을 걷어내고 다시 붙였다. 20개 파일에서 머리말·분량 보고·후속 제안·수평선을 지웠고 본문은 건드리지 않았다. 결과는 **18승 0패, 갈림 6** 이다. 단문에서는 애초에 스킬 쪽에 마무리 문구가 더 많았는데도(32건 중 9건 대 7건) 28승 0패였다.

**목표 문장에 비춘 결론.** "im-not-ai 수준이 되거나 그보다 높은 수준" 에서 **그보다 높은 쪽이다.** 56쌍에서 진 쌍이 없다.

**한계.** 생성 모델이 claude-opus-5 하나다. 판정자를 사람이 아닌 모델이 맡았고 2차 판정은 생성 모델과 같은 opus-5 다(1차의 fable-5-1 은 계정 사용량 소진으로 48회 중 34회가 실패했다). 다만 두 조건 모두 같은 모델이 쓴 글이므로 어느 한쪽으로 기울 자리는 아니다. 56쌍 중 7쌍은 순서에 따라 답이 뒤집혔다. 윤문 파이프라인 재현에서 오케스트레이터의 게이트 스크립트를 붙이지 않았는데, 변경률이 최대 13% 라 그 게이트가 발동할 자리가 없었다.

**C10 장문에서 드러난 결함 둘 (2026-09-10).** 단문 실험만 했으면 못 봤을 것이다.

**요청 분량을 지키지 않았다.** 1,500자를 요청한 장문 24건에서 규칙 없이 쓴 글의 한글이 중앙값 1,205자인데 규칙을 붙인 글은 1,128자였고 1,200자를 넘긴 것이 12건 대 8건이었다. 규칙이 길어지면서 할 말을 줄인 것으로 본다. `SKILL.md` 에 「요청받은 분량을 지킨다」를 넣어 1,165자가 됐다. 여전히 규칙 없는 쪽보다 짧다.

**`것` 구문이 오히려 늘었다.** 1000자당 0.14에서 0.53으로 올랐다. `~라는 것이다`·`~다는 것이다` 로 문단을 닫는 자리였고, 규칙에 예문이 하나뿐이라 그 자리를 못 짚었다. 실제로 나온 문장을 예문으로 넣으니 0.04가 됐다.

**요청 분량은 아직 남았다.** 소제목과 마무리 안내를 걷어내니 한글 중앙값이 1,165자에서 945자로 더 떨어졌다. 1,500자를 요청한 자리에서 규칙 없이 쓴 글은 1,205자, im-not-ai 윤문본은 1,209자다. 판정 기준에 길이가 없어 승패에는 영향이 없었지만, 요청한 분량을 안 지키는 것은 그 자체로 결함이다.

「요청받은 분량은 본문으로 채운다」 를 「요청한 글만 낸다」 절에 넣는 판을 써 봤으나 **판정으로 검증하지 못했다.** 그 시점에 계정 세션 한도에 걸렸다. 측정한 판과 배포하는 판이 달라지면 위의 49승 0패가 무엇에 대한 숫자인지 흐려지므로 그 편집을 되돌렸다. 지금 저장소에 있는 `SKILL.md` 가 49승 0패를 낸 바로 그 판이다. 다음에 할 일은 그 문구를 다시 넣고 `docs/experiments/skill-vs-imnotai/run.sh` 를 장문 프롬프트로 돌려 분량이 오르는지, 승률이 그대로인지 함께 보는 것이다.

**`것` 구문도 되돌아왔다.** 1000자당 0.04에서 0.22로 올랐다(im-not-ai 0.14). 소제목을 빼고 문단으로 이어 쓰면서 설명을 닫는 자리가 늘어난 것으로 본다. 확인하지 않은 추정이다.

**측정에서 두 번 틀렸다.** 처음 분량을 잴 때 빈 파일 넷을 중앙값에 넣어 991자로 보고했다. 빼면 1,078자다. 빈 파일은 `xargs` 로 병렬 실행할 때 `claude` 가 작업 목록 stdin 을 물어 마지막 묶음이 통째로 실패한 것이고, `< /dev/null` 로 끊으니 넷 다 1,193~1,354자로 나왔다. 판정 96건 가운데 35건이 파싱에 실패한 것도 계정 사용량 소진 메시지가 JSON 자리에 들어온 것이었다.

## D. 구조 건전성

| #   | 기준                                           | 임계                      |
| --- | ---------------------------------------------- | ------------------------- |
| D1  | 외부 네트워크 호출                             | **0건**                   |
| D2  | 상시 컨텍스트 비용                             | **1,500 토큰 이하**       |
| D3  | 회귀 테스트가 실제 결함을 잡는가 (변이 테스트) | 주입한 결함 **전부 검출** |
| D4  | 플러그인이 로드되고 스킬 6종(이 저장소 3, im-not-ai 내장 3)과 에이전트 3종이 인식되는가 | **로드됨**                |
| D5  | 상시 규칙 주입이 세션 시작을 막지 않는가       | **어떤 경우에도 exit 0**  |

## E. 실패 모드

| #   | 기준                                                             |
| --- | ---------------------------------------------------------------- |
| E1  | 훅이 죽어도 편집이 막히지 않는다 (잘못된 입력·빈 입력·거대 파일) |
| E2  | 격식 문서(계약·약관)에 대해 예외가 명시돼 있다                   |
| E3  | 오탐이 났을 때 사람이 무시할 경로가 있다                         |

---

## F. 사용하는 사람의 기준 (2026-09-10 신설)

설치해서 1년 넘게 쓰는 사람의 자리에서 본 기준이다. 검사기는 정확해도 쓰기 불편하면 꺼진다.

| #   | 기준                                                                                          | 상태                                                                                                                                                                        |
| --- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | 설치가 명령 두 줄로 끝나고 설정할 것이 없는가                                                 | 통과. 2026-09-10 공개 저장소 생성 후 격리 HOME 에서 `claude plugin marketplace add IsthisLee/claude-korean-writing` 과 `install` 을 실행했고 설치본의 훅이 실제로 검사한다 |
| F2  | 파일 하나, 세션 하나, 플러그인 전체 단위로 끌 수 있는가                                       | 통과. 파일 머리 표시, `KOREAN_WRITING_HOOK_DISABLED=1`, `claude plugin disable`                                                                                             |
| F3  | 이미 써 둔 문서와 CI 에서 훅과 같은 검사를 돌릴 수 있는가                                     | 통과. `scripts/check.sh`. GitHub Actions 첫 실행 성공(2026-09-10)                                                                                                           |
| F4  | 저장소의 산문 문서 전부가 자기 훅을 통과하는가 (검사기가 자기 글을 못 지키면 사람이 무시한다) | 통과. 산문 문서 13종 통과. 2026-09-10 이전의 통과는 공허했다 — 아래 17번을 보라                                                                                                                   |
| F5  | 영어 문서 안의 한국어 문단도 잡는가                                                           | 통과. 한글 비중 30% 이상인 줄만 모아 재판정                                                                                                                                 |
| F6  | 실행 환경이 빠져도 작업을 막지 않는가                                                         | 통과. python3 가 없으면 검사 없이 통과                                                                                                                                      |
| F7  | 상시 컨텍스트 비용을 스킬 6종과 에이전트 3종 기준으로 다시 쟀는가                              | 통과. 격리 HOME 기준 스킬 설명 70·230·60·70 으로 약 430, 에이전트 102·107·88 로 297. 실제 HOME 에서는 에이전트가 773 으로 잡힌다(아래 D2 절)                                                 |

## G. 자리별 비용과 성능 (2026-09-11 신설)

한국어가 나오는 네 자리를 한자리에 놓고 잴 수 있는 것만 쟀다. **품질 숫자는 자리마다 잣대가 달라 서로 줄 세울 수 없다.** 상시 규칙은 주입을 켠 답과 끈 답을 자기끼리 붙인 값이고, 스킬은 im-not-ai 윤문본과 붙인 값이며, 검사 훅은 검출률과 오탐률이다. 자리끼리 견줄 수 있는 것은 토큰과 시간뿐이다.

**측정 조건.** `claude -p --strict-mcp-config --max-turns 1 --output-format json` 의 `usage` 에서 `input_tokens` 와 `cache_creation_input_tokens` 와 `cache_read_input_tokens` 를 더했다. 사용자 설정은 빼지 않았다. `--setting-sources ""` 를 붙이면 심볼릭 링크로 로드된 이 플러그인의 SessionStart 훅이 돌지 않아 주입을 켠 실행과 끈 실행이 같은 값을 낸다. 기준선 프롬프트는 `1+1은?` 이고 총 26,515 다. 절대값은 세션 조건에 따라 달라지므로 차이만 본다.

### 상시로 드는 것

세션을 열기만 해도 들어가는 몫이다.

| 항목 | 토큰 | 어떻게 쟀나 |
| --- | --- | --- |
| 상시 규칙 주입 | **726** | `KOREAN_WRITING_ALWAYS_ON_DISABLED` 를 껐다 켜서 총량 차이를 봤다. 27,241 대 26,515. 2회 반복 모두 726 |
| 스킬 설명 넷 | 약 430 | `/context` 의 스킬 표. korean-writing 70, humanize-korean 230, korean-character-count 60, crafting-effective-readmes 70 |
| 에이전트 셋 | 773 | `/context` 의 Custom Agents 표. 266·273·234. 격리 HOME 에서는 297 로 잡힌다 (D2 절) |
| 슬래시 전용 스킬 둘 | **0** | `humanize` 와 `humanize-redo` 는 `disable-model-invocation: true` 라 `/context` 의 스킬 표에 실리지 않는다. 이름을 쳐야만 뜨는 대신 상시 몫이 없다 |

726 은 D2 절에 적힌 값이 그대로 재현된 것이다. 재는 방법이 맞는지는 옛 규칙으로 662 가 나오는지 먼저 보면 안다.

### 요청할 때만 드는 것

스킬 본문을 프롬프트에 실어 기준선과의 차이를 봤다. 꼬리 문구 몫으로 15 토큰 안팎이 섞여 있으므로 끝자리는 믿지 않는다.

| 파일 | 토큰 | 바이트 | 언제 열리나 |
| --- | --- | --- | --- |
| `SKILL.md` | 약 10,100 | 21,481 | 글 작성 요청 |
| `skills/humanize-korean/SKILL.md` | 약 14,100 | 29,169 | 윤문 요청 |
| `skills/korean-character-count/SKILL.md` | 약 1,130 | 2,524 | 글자 수 요청 |
| `skills/crafting-effective-readmes/SKILL.md` | 약 940 | 2,676 | README 요청 |
| `hooks-handlers/always-on.md` | 726 | 1,572 | 세션마다 |

글 작성 요청 하나에 실제로 드는 것은 상시 몫 약 1,930 에 본문 10,100 을 더한 12,000 안팎이다. 윤문은 본문 14,100 이 콜 수만큼 곱해진다.

### 검사 훅의 지연

훅은 bash 안에서 python3 정규식을 돌린다. LLM 을 부르지 않으므로 토큰은 0 이고 남는 것은 지연뿐이다. 같은 입력을 10회씩 넣어 중앙값을 냈다.

| 편집분 | 중앙값 | 최소 | 최대 |
| --- | --- | --- | --- |
| 한글 730자 | 35.3ms | 33.2ms | 36.7ms |
| `SKILL.md` 전체 (10,102자) | 38.3ms | 36.2ms | 40.2ms |
| `README.md` 전체 (35,643자) | 42.5ms | 39.7ms | 47.7ms |

`scripts/check.sh` 로 문서 14종을 한 번에 검사하면 1.01초다. 파일마다 python3 를 새로 띄우므로 파일당 약 72ms 다. 훅 등록의 제한 시간은 10초인데 가장 큰 문서가 그 0.5% 도 쓰지 않는다.

### 윤문 두 경로

| 경로 | 입력 | 걸린 시간 | 비용 | 턴 | 변경률 | 등급 | 자체검증 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Fast 1콜 (light) | 배포 공지 280자 | **130초** | **0.64달러** | 11 | 1.1% | A | 6/6 |
| 정밀 3콜 (standard 승급) | AI 티를 몰아넣은 200자 | 451초 | 1.64달러 | 16 | 39.0% | A- | 6/6 |

Fast 경로는 2026-09-11 에 쟀다. 조건은 `claude -p "/korean-writing:humanize <본문>" --setting-sources "" --strict-mcp-config --plugin-dir <저장소> --dangerously-skip-permissions --max-turns 40` 이고 격리된 작업 폴더다. 정밀 3콜 기록은 [`docs/samples/humanize-run.md`](docs/samples/humanize-run.md) 에 있다.

**입력이 이미 깨끗하면 거의 손대지 않는다.** Fast 경로가 실제로 고친 것은 연결어미 뒤 쉼표 3개뿐이다. `~를 통해` 와 문두 `또한` 과 `~할 수 있다` 는 각각 1회라 빈도 기준에 못 미쳐 그대로 뒀고, 관형절 중첩 1건은 어절 재배치가 필요해 보존 판단으로 남겼다. 변경률 1.1% 는 게이트의 경고선 30% 와 폐기선 50% 에서 멀다.

### 자리별 품질 근거

| 자리 | 근거 | 어디에 |
| --- | --- | --- |
| 상시 규칙 | 주입 유무 블라인드 24쌍에서 21승 3무 0패 | C5 |
| `korean-writing` 스킬 | im-not-ai 윤문본과 블라인드 56쌍에서 49승 0패 | C9 |
| 윤문 | 이 저장소가 만든 것이 아니라 im-not-ai 의 실측에 기댄다. 여기서 보는 것은 변경률 게이트와 자체검증 통과 여부다 | 위 표 |
| 검사 훅 | 위반 10/10, 정상 오탐 0/5, 실문서 2023년 이전 32개 중 1개 | A1·A2·아래 |

**2026-09-11 실문서 재측정.** `scripts/measure.sh ~/Documents` 로 210개를 검사해 84개가 걸렸다(K1 74, K9 28, K10 17, K8 10, K4 8, K7 5, K3 4, K5 4, K2 1). 9.7초 걸렸다. 파일 수정 연도로 가르면 2023년 이전 32개 중 1개, 2024~2025년 1개 중 0개, 2026년 177개 중 83개다. 걸린 84개 가운데 20개가 이 저장소 문서인데 전부 im-not-ai 참조 문서이거나 실험이 남긴 옛 규칙 파일이고, 이 저장소의 산문 문서는 하나도 걸리지 않았다. 코퍼스가 143개에서 210개로 늘어난 것은 im-not-ai 내장으로 저장소 자체의 `.md` 가 늘었기 때문이다.

## 측정 결과 (2026-09-09)

| 기준                | 결과                                                    | 판정                          |
| ------------------- | ------------------------------------------------------- | ----------------------------- |
| A1 위반 검출        | 10/10                                                   | 통과                          |
| A2 정상 오탐        | 0/5                                                     | 통과                          |
| A3 실문서 오탐      | 1/143 (0.7%)                                            | 통과. 2026-09-10 재측정, 아래 |
| A4 분류 정확        | 10/10                                                   | 통과                          |
| B1 트리거 매칭      | 5/5                                                     | 통과                          |
| B2 코드 작업 오매칭 | 0/5                                                     | 통과                          |
| C1 처방 존재        | 8/8                                                     | 통과                          |
| C6 규칙집 커버리지  | quick 61항목 중 60 (개정 전 21)                         | 통과                          |
| C7 생성 단계 비교   | 블라인드 쌍대 12승 4패 대 옛 스킬 (p=0.038)             | 통과                          |
| C10 장문 결함       | 분량 미준수·것 구문 증가 둘 다 규칙 보강 후 해소          | 통과                          |
| C9 im-not-ai 대비   | 56쌍 49승 0패 (단문 28-0, 장문 21-0)                     | 통과 (우월)                   |
| C8 상시 규칙 회귀   | regress.sh 새 2승 옛 2승, 종료 0                        | 통과 (나빠지지 않음)          |
| K9·K10 사람 글 오탐 | 0/32 (2023년 이전 문서)                                 | 통과                          |
| C3 자기 일관성      | 위반 0건                                                | 통과                          |
| D1 외부 호출        | 0건                                                     | 통과                          |
| D2 상시 비용        | 약 1,450 토큰 (상시 규칙 726 + 스킬 설명 430 + 에이전트 297) | 통과 (기준 1,500). 환경 주의  |
| D3 변이 검출        | 15/15. 2026-09-10 새 판정 8종 추가 주입, 8/8            | 통과                          |
| D4 플러그인 로드    | loaded, 스킬 6종 + 에이전트 3종 (2026-09-10 격리 HOME 재확인) | 통과                          |
| D5 주입 안전성      | 규칙 파일 없음·끄기 두 종 전부 exit 0                   | 통과                          |
| C5 주입 효과        | 블라인드 21승 0패 3무 (24쌍)                            | 통과                          |
| E1 이상 입력        | 5종 전부 exit 0                                         | 통과                          |
| 회귀 테스트         | posttooluse 55/55, sessionstart 24/24                   | 통과                          |

2026-09-10 재측정. 이 머신에 이 플러그인과 무관하게 쌓인 한국어 `.md` 143개(한글 20자 이상)를 `scripts/check.sh` 로 통째로 넣었다. 걸린 60개 중 59개는 2026년에 Claude 가 쓴 문서(CLAUDE.md, 구현 로그, QA 보고서. 대부분 K1 줄표)이고 사람이 쓴 문서는 1개다(외부에서 가져온 지식 정리 문서, K3·K8). 아래 10·11번을 고치기 전에는 사람이 쓴 문서 7개(4.9%)가 걸렸다.

상시 비용은 두 가지로 쟀다. 윤문 스킬이 있던 때의 스킬 설명 넷은 `claude -p "/context"` 가 내는 스킬별 표에서 70·70·90·60, 합계 약 290 이었다. `korean-writing` 설명을 글 작성 요청으로 넓히기 전에는 50 이었고 합계는 270 이었다. 상시 규칙은 설치된 플러그인을 그대로 두고 `KOREAN_WRITING_ALWAYS_ON_DISABLED=1` 로 껐다 켜며 `claude -p` 의 `usage` 입력 토큰 총량을 뺐다. 끄면 12,364, 켜면 13,026 으로 차이가 662 다. 스크래치 사본이 아니라 실제 훅 경로로 잰 값이다. 이 방법이 예전에는 실행마다 2,143토큰씩 흔들려 쓸 수 없었는데, `--strict-mcp-config` 로 MCP 도구 정의를 빼고 `--setting-sources ""` 로 사용자 설정을 빼니 값이 고정됐다.

윤문 파이프라인을 im-not-ai 에서 내장한 뒤 격리된 HOME 에 설치해 다시 재니, 스킬 설명은 70·230·60·70 으로 약 430 이고(슬래시 전용 스킬 둘은 목록에 실리지 않는다), 에이전트 셋이 297, 상시 규칙은 그대로 662 다. 합계 약 1,390 이다.

**항목별 추정은 환경에 따라 흔들린다 (2026-09-10 확인).** 같은 에이전트 파일인데 `claude -p "/context"` 의 Custom Agents 표가 격리 HOME 에서 102·107·88(합계 297), 사용자의 실제 HOME 에서 266·273·234(합계 773)로 나온다. 두 값 모두 여러 번 재현된다. 파일은 `diff` 로 같음을 확인했다. 항목별 추정치를 정밀한 값으로 인용하면 안 된다.

**재현되는 것은 상시 규칙 726 하나뿐이다.** 그것만 껐다 켤 스위치가 있어서 입력 토큰 총량을 빼는 방식으로 잴 수 있다. 스킬 설명과 에이전트는 끄고 켤 수단이 없어 `/context` 의 추정에 기댈 수밖에 없다. 격리 HOME 에 인증이 없어 그쪽에서는 API 호출 자체가 안 되므로(`terminal_reason: api_error`) 토큰 차이 측정도 못 한다.

그래서 D2 는 격리 HOME 기준 약 1,450 으로 둔다. 실제 사용 환경에서는 에이전트 몫이 더 크게 잡히므로 여유가 표보다 적다고 보는 편이 안전하다. 다음에 이 값을 다시 쓸 일이 있으면 어느 환경에서 잰 값인지 먼저 적는다.

**im-not-ai 원본과의 비교 (2026-09-10).** 원본을 커밋 9747f03 으로 받아 격리 HOME 에 설치하고 같은 표를 봤다.

| | 에이전트 수 | description 합계 | `/context` 토큰 |
| --- | --- | --- | --- |
| im-not-ai 원본 | 9 | 2,336자 | 899 |
| 이 저장소가 내장한 것 | 3 | 785자 | 297 |
| 싣지 않은 개발용 | 6 | 1,551자 | 약 600 |

개발용 여섯은 `korean-ai-tell-taxonomist`, `korean-translation-scholar`, `post-editese-metric-engineer`, `quick-rules-integrator`, `taxonomy-gap-analyzer`, `translationese-research-distiller` 다. 규칙집을 만들고 늘리는 데 쓰는 것이라 윤문을 돌릴 때는 부르지 않는다. 그래서 내장에서 뺐고, 그 결정이 상시 컨텍스트에서 약 600 토큰을 아낀다.

## 측정 중 고친 것

1. **한글 임계 100자 → 20자.** 실제 문단의 중앙값이 22자인데 임계가 100자라 92%를 걸러냈다. 첫 측정에서 검출률 0/10 이 나온 원인이다.
2. **K1 을 줄표 개수에서 삽입구 정규식으로.** 표 셀의 `| 항목 | PASS — 근거 |` 를 오탐했다. 표 행을 제외하고 `[^|\s]\s[—–]\s[^|\s]` 만 센다.
3. **K2 조사 확장 + `축` 오매칭 방지.** `레이어도` 를 놓쳤고, 압축·건축의 `축이` 를 잡을 뻔했다.
4. **K6 임계 1 → 2.** taxonomy D-8 의 처방이 "한 문서 1회 이하" 인데 1회에서 걸려 기준보다 엄격했다. (당시 규칙집은 claude-forge 판이었다. 지금의 im-not-ai 판에는 승패 의인화 항목이 없고, 처방은 SKILL.md 에 있다. 14번 참조.)
5. **K7·K8 신설.** 사물 의인화와 번역투 패턴이 아예 없어 정답 10건 중 4건을 놓쳤다.
6. **회귀 테스트 전면 재작성.** K1 을 정규식으로 바꾼 뒤 테스트를 갱신하지 않아 실패 상태였고, 그 탓에 변이 테스트가 거짓 통과했다.
7. **횟수 검증 도입.** 정규식 대안(`A|B|C`) 하나가 빠져도 다른 대안이 케이스를 살려 놓쳤다. 보고 횟수를 검증해 대안별 민감도를 얻었다.

8. **표 행 전체를 검사에서 제외.** 처음엔 K1(줄표)만 표를 뺐는데, README 의 전/후 교정표에서 '전' 칸의 나쁜 예가 K3·K7 로 걸렸다. 나쁜 예를 인용하는 문서가 그 예 때문에 걸리면 안 된다. 표는 산문이 아니므로 전 항목에서 뺐다. 회귀 케이스 1건 추가(27→28).

9. **영어가 대부분인 편집 안의 한국어 문단.** 편집분 전체의 한글 비율 30%로 잘라서, 영어 문서에 한국어 문단 하나가 섞인 편집은 검사하지 않았다. 한글 52자, 비율 0.04인 편집에 K6 2회와 K4가 있어도 exit 0이었다. 전체 비율이 미달이면 한글 비중 30% 이상인 줄만 모아 같은 기준으로 다시 본다. 영어 줄은 빠지므로 영어 산문의 줄표는 세지 않는다. 회귀 케이스 4건 추가(28→32).

10. **K7 에서 "죽다" 제외.** 서버가 죽다·프로세스가 죽다는 개발자의 일상어인데 사물 의인화로 잡았다. 실측 문서에서 "서버가 죽었", "훅이 죽어" 가 자연스러운 문맥으로 나왔고, 안내 문구조차 "죽는다" 를 정상 어휘로 들고 있었다. 문구도 "굳다·쓰러지다 같은 직역 비유 대신 멈춘다·죽는다처럼 사실대로" 로 고쳤다.

11. **K8 에서 "~에 대해"·"~를 통해" 횟수 판정 제외.** 실측 143개 중 3회 이상인 문서 10개가 전부 사람이 2022~23년에 쓴 블로그 글과 정리 노트였고, 2026년에 Claude 가 쓴 문서는 하나도 걸리지 않았다. 판별력이 없는 규칙은 사람의 글만 잡는다. 생성 단계의 회피는 스킬(품질 바)이 그대로 맡는다. 이중 피동·`가지고 있다`·`~에 의해` 는 남긴다. 정답 G10 은 여전히 잡힌다.

12. **제외 구간에 `~~~` 울타리 코드블록과 HTML 주석 추가.** 코드블록을 ``` 만 걷어내고 있었다. 회귀 케이스 4건 추가(36→40).

13. **줄표는 파일 전체로 판정.** 편집분만 세니 문단 하나씩 고치는 동안 쌓이는 줄표를 한 번도 못 잡았다. 실측 문서에서 줄표 34개, 66개, 207개짜리 파일이 그렇게 만들어졌다. 이번 편집에 줄표 삽입구가 하나라도 있으면 파일 전체(같은 제외·한국어 규칙)의 개수로 판정한다. 이번 편집에 없으면 파일에 아무리 많아도 잡지 않으니 소음은 그대로다. 회귀 케이스 3건 추가(40→43).

14. **규칙집과 압축 룰북을 im-not-ai 원본으로 교체 (2026-09-10).** 둘 다 claude-forge 를 거친 판이었다. taxonomy 는 forge 가 im-not-ai v2.0 에 D-8·D-9·J-3 상향을 더한 것이었고, quick-rules 는 forge 가 따로 만든 압축본이었다. im-not-ai 커밋 9747f036cdc2 (2026-09-06) 의 두 파일로 바꾸고 머리에 훅 제외 표시만 붙였다. 항목은 73개에서 84개(보류 1건 제외)가 됐다. 훅의 정규식은 손대지 않았으므로 판정은 바뀌지 않는다(회귀 43/43). 같은 디렉터리를 오늘 다시 재니 한국어 문서 169개 중 66개가 걸렸다(K1 58, K8 6, K3 5, K4 5, K7 5, K5 2, K2 0, K6 0). 아침의 143개·60개와 다른 것은 그 사이 문서가 늘었기 때문이고, 걸린 66개 중 6개는 이 저장소 `docs/experiments/` 의 실험 산출물이다. 승패 의인화·추상 구조어의 근거로 적혀 있던 출판사 편집부 일화는 forge 규칙집에만 있던 것이라 SKILL.md·README·훅 주석에서 빼고, 정답 데이터 G05·G06 을 근거로 적었다. `humanize-korean` 스킬은 im-not-ai 의 절차와 철칙을 바탕으로 다시 썼다.

15. **윤문 스킬과 규칙집 사본을 빼고 im-not-ai 를 플러그인 의존성으로 (2026-09-10).** 14번에서 원본으로 바꾼 사본을 다시 뺐다. 같은 이름의 스킬 둘이 뜨는 충돌과 사본 갱신 부담이 이유다. `plugin.json` 에 `dependencies: ["humanize-korean"]`, `marketplace.json` 에 im-not-ai 저장소를 커밋 9747f036cdc2 로 고정한 항목을 넣었다. 격리된 HOME 에서 `claude plugin install korean-writing` 이 "+ 1 dependency: humanize-korean" 으로 2.3.2 를 함께 설치했고, 캐시의 git HEAD 가 고정한 sha 와 같았으며, korean-writing 이 켜진 채로 humanize-korean 만 끄는 것은 막혔다. 훅과 회귀 테스트는 손대지 않았다(43/43, 20/20).

16. **윤문 파이프라인을 im-not-ai 에서 내장 (2026-09-10).** 15번의 의존성 방식은 `/humanize-korean:…` 이름이 그대로 남고 에이전트 아홉 종의 설명이 실렸다. 그쪽 이름을 숨기는 길을 셋 다 시험했다. 마켓플레이스 항목 `strict: false` 로 스킬을 골라 싣기는 "plugin.json 과 항목이 둘 다 구성 요소를 지정해 충돌" 오류로 로드가 실패했고, `skillOverrides` 는 문서대로 플러그인 스킬에 적용되지 않았으며, `permissions.deny` 는 실행만 막고 목록은 그대로였다(격리 HOME 의 터미널 화면을 캡처해 확인). 그래서 런타임 부분집합(스킬 3, 에이전트 3, 스크립트 9, 참조 파일)을 커밋 9747f036cdc2 에서 그대로 넣었다. 격리 HOME 에서 설치 뒤 `/humanize` 자동완성에 `/korean-writing:` 셋만 떴고, 사용자 설정을 뺀 실제 로그인으로 `/korean-writing:humanize` 에 AI 티를 몰아넣은 200자 문단을 넣으니 진단·윤문·마무리 검토 세 콜이 끝까지 돌았다(451초, 1.64달러, 변경률 39.0%, 등급 A-, 자체검증 6/6, 마무리 검토가 서법이 사라진 문장 하나를 되돌림). 기록은 `docs/samples/humanize-run.md` 에 있다. 훅과 회귀 테스트는 손대지 않았다(43/43, 20/20).

17. **제외 표시가 문서 일곱 종의 자기 검사를 통째로 꺼 놓고 있었다 (2026-09-10).** 훅은 `korean-writing: ignore` 라는 문자열이 편집분이나 파일 머리 4,000자 안에 있으면 검사를 건너뛰었다. 문제는 이 기능을 설명하는 문서가 그 문자열을 인용한다는 것이다. README·README.en·CLAUDE.md·CHANGELOG·CONTRIBUTING·CONTRIBUTING.en·SECURITY 일곱이 본문·표·코드블록에서 인용하고 있었고, `scripts/check.sh` 와 CI 의 「한국어 문서가 자기 훅을 통과하는가」 작업이 검사 대상 12개 중 7개를 아무것도 보지 않고 통과시켰다.

    확인은 표시 문자열만 무해하게 깨뜨린 사본을 만들어 다시 검사하는 것으로 했다. 일곱 문서 모두 그 상태에서도 K1~K8 을 통과했으므로 이 결함이 실제 위반을 가려 주고 있지는 않았다. 가려진 것은 위반이 아니라 검사 자체다.

    고친 방식은 줄 하나로 선 `<!-- korean-writing: ignore -->` 만, 그것도 파일 앞 열 줄 안에 있을 때만 지시로 보는 것이다. 실제로 끄려고 붙인 표시 여섯 개는 전부 1행에 줄 하나로 있어 그대로 산다. 문서가 이미 "파일 머리에 넣으라" 고 안내하고 있었으므로 코드를 문서에 맞춘 셈이다. 회귀 케이스 3건 추가.

18. **K9 부정 대구, K10 연결어미 뒤 쉼표 신설 (2026-09-10).** im-not-ai 규칙집에서 실측 판별력이 가장 큰 둘인데 훅에도 스킬에도 없었다. 임계는 이 머신에 쌓인 한국어 `.md` 205개로 정했다. 사람이 쓴 글과 Claude 가 쓴 글의 구분은 파일 수정 연도로 갈랐다(2023년 이전 32개, 2026년 173개). 거친 대리 지표이므로 한계로 적어 둔다.

    | 후보 임계 | 2023년 이전 | 2026년 |
    | --- | --- | --- |
    | 대구 2회+ | 0/32 | 40/173 |
    | 대구 3회+ (채택) | **0/32** | 30/173 |
    | 쉼표 4회+ 이면서 30%+ | 1/32 | 28/173 |
    | 쉼표 6회+ 이면서 30%+ (채택) | **0/32** | 21/173 |
    | 쉼표 6회+ 이면서 35%+ | 0/32 | 13/173 |

    규칙집의 대구 임계는 2회지만 훅은 3회로 뒀다. 훅이 스킬보다 느슨하다는 기존 방침대로다. 쉼표는 개수만 보면 긴 문서가 불리하고 비율만 보면 짧은 문서가 불리해 둘을 함께 넘을 때만 잡는다. 대구 정규식은 규칙집 구현을 따르되 조건절 `아니라면`·`아니라서` 를 뺐다. 가정이지 대구가 아니고, 실제로 생성물에서 "일요일 기준이 아니라면" 이 잡혔다. 문서 205개 판정은 그 변경으로 바뀌지 않는다. 회귀 케이스 9건 추가(43→55).

19. **`scripts/measure.sh` 가 두 자리 코드를 세지 못했다 (2026-09-10).** 집계 정규식이 `K\d` 라 `K10` 줄이 어느 코드에도 안 잡혔다. 파일은 "걸린 파일" 로 세면서 코드별 표에서는 사라진다. K10 을 넣고 처음 돌린 측정에서 코드별 표에 K10 이 없어 발견했다. `K\d+` 로 고쳤다.

20. **저장소의 산문 문서를 새 판정에 맞춰 고쳤다 (2026-09-10).** 17번을 고치고 나니 README(K9 5회, K10 40회), EVALUATION(K10 16회), CHANGELOG(K10 16회), CONTRIBUTING(K9 3회)이 걸렸다. 연결어미 뒤 쉼표는 86개를 지웠고 부정 대구는 문서마다 둘 이하로 줄였다. 이 저장소의 문서가 이 머신 전체에서 연결어미 쉼표 비율이 가장 높았다(EVALUATION 57%, CHANGELOG 54%, README 46%).

21. **같은 규칙이 놓인 세 곳이 어긋났다 (2026-09-10).** 규칙 본문은 `SKILL.md` 가 정본이지만 사본이 둘 더 필요하다. 상시 규칙은 스킬이 안 뜨는 평소 답변을 맡고 `CLAUDE.md` 는 훅도 스킬도 전달되지 않는 서브에이전트를 맡는다(N1 실측). 플랫폼이 강요한 중복이라 없앨 수 없다. 이번에 대구와 연결어미 쉼표를 앞의 둘에만 넣고 `CLAUDE.md` 를 빠뜨려서 실측 최강 신호 둘이 서브에이전트에만 안 걸릴 뻔했다. 채워 넣고 `test_sessionstart.py` 에 세 곳 동기화 검사를 더했다(22→24건). 상시 규칙에서 대구 줄을 지워 보니 `빠진 항목: ['대구', '연결어미 뒤 쉼표']` 로 잡는다.

22. **이 문서의 한 절이 편집 중에 통째로 사라졌다 (2026-09-10).** 「측정 중 고친 것」 절 열여섯 건이 C9 절을 다시 쓰는 과정에서 지워졌고, `## D. 구조 건전성` 과 `## 상시 규칙을 넣으며 남겼던 것` 헤딩이 각각 두 번씩 붙어 있었다. README 를 실제 구현과 전수 대조하다가 "그렇게 바꾼 열세 건" 이라는 문장의 근거를 세려다 발견했다. git 에서 원문을 되살렸다. 문자열 치환으로 절을 갈아 끼울 때는 앵커가 남는지 헤딩 목록으로 확인한다.

## 상시 규칙을 넣으며 남겼던 것 (전부 끝남)

C5 를 넣을 때 못 잰 것 셋이다. 2026-09-10 에 전부 쟀고 결과를 아래에 적었다. 표는 무엇을 물었고 어떻게 답이 났는지다.

| #  | 무엇을 모르는가                                        | 재는 방법                                                                                                                                                    | 왜 중요한가                                                                                  |
| -- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| N1 | ~~서브에이전트~~ **결론 남**                                | 훅도 스킬도 전달되지 않는다. `CLAUDE.md` 만 전달된다. 플러그인 범위 밖으로 두고 쓰는 쪽 `CLAUDE.md` 에 넣도록 안내한다        | 아래에 실측을 적었다                                                                        |
| N2 | ~~긴 대화~~ **결론 남**                                  | 30턴 뒤에도 효과가 남는다. 아래에 실측을 적었다                                                                            | 긴 세션에서 없는 것과 같아지면 안 된다                                                       |
| N3 | ~~회귀 감시 수단~~ **끝남**                             | `docs/experiments/always-on/regress.sh`. 옛 규칙과 새 규칙으로 같은 프롬프트를 돌려 블라인드로 비교한다                     | 규칙을 고쳤을 때 나빠진 것을 잡을 수단이 없었다                                              |

**N1 은 끝났다 (2026-09-10).** SessionStart 훅이 낸 추가 컨텍스트는 서브에이전트에 전달되지 않는다. 같은 방식의 SessionStart 훅에 추측할 수 없는 토큰을 싣고 그 토큰을 모르는 지시문으로 서브에이전트에게 물었다. 부모 세션은 토큰을 그대로 냈고 서브에이전트는 두 번 다 `NONE` 을 냈다. 문구를 바꿔 두 번 돌린 결과가 같다. 훅이 돌고 출력만 안 가는 것인지 훅 자체가 안 도는 것인지도 갈랐다. 실행마다 파일에 한 줄을 남기는 SessionStart 훅을 matcher 없이 걸고 세 번 돌렸는데, 서브에이전트를 둘 띄운 실행에서도 기록이 한 줄이었고 `source` 가 `startup`, `agent_id` 가 비어 있었다. 서브에이전트를 위해서는 이 훅이 돌지 않는다. 출력이 갈 자리가 애초에 없다. 덤으로 서브에이전트가 늘어도 주입이 반복되지 않으니 추가 비용도 없다.

이어서 세 경로를 더 쟀다. 서브에이전트 시작 훅의 stdout 도 전달되지 않는다(훅이 돌았다는 것은 같은 명령에서 파일에 남긴 기록으로 확인했다). 플러그인 스킬 목록도 전달되지 않는다(`korean-writing` 이 목록에 있느냐는 물음에 `NO`). 전달되는 것은 `CLAUDE.md` 하나뿐이다(사용자 지침의 문장을 그대로 인용해 돌려줬다).

플러그인이 서브에이전트에 넣을 경로가 없다. 그래서 범위 밖으로 두고 맡고 싶은 사람에게 `CLAUDE.md` 한 줄을 안내한다. 이 저장소의 `CLAUDE.md` 에 같은 줄을 넣어 본보기로 뒀다.

**N2 도 끝났다 (2026-09-10).** 30턴을 채운 뒤에도 주입 조건이 이긴다.

한 세션에 채움 질문 30턴과 긴 답변 넷을 쌓아 컨텍스트를 3만에서 4만 토큰까지 키운 뒤, 그 대화에서 한 번도 안 나온 프롬프트 넷(02·06·07·12)을 물었다. 주입 조건과 기준 조건을 같은 깊이에서 나란히 돌리고 순서를 바꿔 두 번씩 블라인드로 판정했다.

| 측정                     | 기준     | 주입    |
| ------------------------ | -------- | ------- |
| 블라인드 쌍대 승 (4쌍)   | 0        | **4**   |
| 무승부                   | 0        | 0       |
| 훅 검사기 적중           | 0        | 0       |
| 굵게 `**` 사용           | 29       | **7**   |
| 불릿 줄                  | 14       | **5**   |

네 쌍 모두 두 순서에서 같은 답이 나왔다. 주입문이 대화 맨 앞에 있어도 30턴 뒤까지 힘이 남는다. 주입 조건의 세션에 마지막으로 물어보니 규칙 첫 줄을 그대로 냈고 기준 조건은 `NONE` 을 냈다.

**설계에서 한 번 틀렸다.** 처음에는 같은 프롬프트를 대화 앞과 뒤에서 각각 물어 비교하려 했다. 그러면 뒤의 답이 앞의 답을 그대로 되풀이한다. 기준 조건 네 건이 글자까지 같았다. 앞뒤 비교를 버리고 같은 깊이에서 두 조건을 나란히 놓는 방식으로 바꿨다.

**한계.** 프롬프트가 넷이고 표본이 1이다. 채움 질문이 한 줄짜리 산수라 실제 작업 대화와 다르다. 30턴에서 봤고 그보다 긴 대화는 재지 않았다.

**N3 도 끝났다 (2026-09-10).** `docs/experiments/always-on/regress.sh` 를 만들었다. 옛 규칙과 새 규칙으로 같은 프롬프트를 각각 생성하고 어느 쪽이 새 규칙인지 모르는 판정자에게 순서를 바꿔 두 번 물어 이긴 쪽을 센다. 새 규칙이 진 쌍이 더 많으면 종료 코드 1 이다.

채택 시점의 답변을 얼려 두고 대조하는 방식은 버렸다. 생성 비용이 절반이지만 모델이 바뀌면 그 차이까지 회귀로 잡힌다. 두 규칙을 같은 시각 같은 모델로 돌리면 규칙 차이만 남는다.

프롬프트 넷 기준으로 생성 8회와 판정 8회, 3~4달러다. LLM 을 부르므로 CI 에는 넣지 않고 릴리스 전에 사람이 한 번 돌린다. 높임 규칙을 넣기 전 판과 지금 판을 프롬프트 07 로 비교해 실제로 돌려 봤고 새 규칙이 이겨 종료 코드 0 이 나왔다.

C5 실험 자체의 한계는 그 절에 적어 두었다. 판정 모델과 생성 모델이 같고 표본이 2다. 기준선 환경에서 사용자 설정을 뺐다.

## 재현

```bash
python3 hooks-handlers/test_posttooluse.py     # 회귀 55건
python3 hooks-handlers/test_sessionstart.py    # 상시 규칙 회귀 24건
scripts/measure.sh ~/Documents                 # 실제 문서 뭉치의 오탐 측정
docs/experiments/skill-vs-imnotai/run.sh       # 스킬 대 im-not-ai 블라인드 판정
docs/experiments/always-on/regress.sh 옛.md 새.md  # 상시 규칙 회귀
```

앞의 셋은 LLM 을 부르지 않아 언제든 돌립니다. 뒤의 둘은 부르므로 릴리스 전에 한 번 돌립니다. C7·C9 의 판정은 `skill-vs-imnotai` 가 그대로 재현합니다.

실문서 오탐은 분기마다 다시 잰다. `scripts/measure.sh` 로 실제 문서가 쌓인 디렉터리를 돌리고 걸린 파일을 사람이 쓴 글과 Claude 가 쓴 글로 나눠 이 문서에 날짜와 함께 적는다. 사람이 쓴 글의 비율이 1% 를 넘으면 그 코드의 규칙을 다시 본다. 2026-09-10 측정은 이 방법으로 했다(이 저장소의 문서를 포함하면 154개 중 60개, 사람 글 1개).

정답 데이터는 `hooks-handlers/ground-truth.json`(위반 10건)과 `clean.json`(정상 5건)이다.
실제로 생성됐던 어색한 문장에서 가져왔다. 합성 예문이 아니다.
