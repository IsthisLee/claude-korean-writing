파이썬 함수 하나를 써라.

topk_by(items, key, k) -> list
  key(item) 이 큰 순으로 위에서 k 개를 돌려준다.
  값이 같으면 원래 순서를 지킨다(안정 정렬).
  k 가 items 길이보다 크면 전부 돌려준다. k 가 0 이하면 빈 리스트다.
  items 를 바꾸지 않는다.

표준 라이브러리만 쓴다. 도구를 쓸 수 없으니 실행해 보지 말고 써라.
설명 없이 ```python 코드블록 하나만 내라. 함수 밖의 예제나 print 는 넣지 마라.
