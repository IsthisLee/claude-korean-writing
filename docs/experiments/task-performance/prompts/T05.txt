자바스크립트에서 아래가 콘솔에 찍는 값을 순서대로.

for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 0); }

마지막 줄에 ANSWER=<쉼표로 이은 값> 형식으로 적어라. 예: ANSWER=0,1,2

도구를 쓸 수 없다. 실행해 보지 말고 추론만으로 답하라. 반드시 마지막 줄에 위 형식으로 ANSWER= 한 줄을 적어라.
