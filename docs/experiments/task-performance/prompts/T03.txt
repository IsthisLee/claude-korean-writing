아래 SQL 에서 orders 가 하나도 없는 고객도 결과에 나오는가?

SELECT c.id FROM customers c
LEFT JOIN orders o ON o.customer_id = c.id
WHERE o.status = 'paid';

마지막 줄에 ANSWER=YES 또는 ANSWER=NO 로 적어라.

도구를 쓸 수 없다. 실행해 보지 말고 추론만으로 답하라. 반드시 마지막 줄에 위 형식으로 ANSWER= 한 줄을 적어라.
