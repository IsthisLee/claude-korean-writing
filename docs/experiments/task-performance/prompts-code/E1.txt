파이썬 함수 하나를 써라.

merge_intervals(intervals) -> list[tuple[int, int]]
  [(start, end), ...] 를 받아 겹치거나 맞닿는 구간을 합쳐 start 오름차순으로 돌려준다.
  입력은 정렬돼 있지 않을 수 있다. 빈 입력은 빈 리스트다.
  (1, 3) 과 (3, 5) 처럼 끝이 맞닿으면 합친다.

표준 라이브러리만 쓴다. 도구를 쓸 수 없으니 실행해 보지 말고 써라.
설명 없이 ```python 코드블록 하나만 내라. 함수 밖의 예제나 print 는 넣지 마라.
