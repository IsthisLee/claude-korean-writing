파이썬 함수 하나를 써라.

parse_duration(text) -> int
  "1h30m", "45s", "2d", "1h 30m 10s" 같은 문자열을 초 단위 정수로 바꾼다.
  단위는 d, h, m, s 이고 순서는 큰 것부터 오지만 일부가 빠질 수 있다.
  공백은 있어도 되고 없어도 된다. 숫자는 음수가 아닌 정수다.
  형식에 맞지 않으면 ValueError 를 낸다. 빈 문자열도 ValueError 다.

표준 라이브러리만 쓴다. 도구를 쓸 수 없으니 실행해 보지 말고 써라.
설명 없이 ```python 코드블록 하나만 내라. 함수 밖의 예제나 print 는 넣지 마라.
